{
  "name": "ovoo-chrome-extension",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "tsc --noEmit",
    "release": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "typescript": "^6.0.3",
    "vite": "^8.0.6",
    "vite-plugin-web-extension": "^4.5.1"
  }
}
